[compiler]
inline-mir = "off"

[analysis]
report-roots = [
    "optimizer_pruning::const_false_call",
    "optimizer_pruning::documented_inlinable_call",
]

[panics]
panic-sink-namespaces = [
    "core::panicking::**",
    "std::panicking::**",
    "core::std::rt::panic_fmt",
    "std::rt::panic_fmt",
]
