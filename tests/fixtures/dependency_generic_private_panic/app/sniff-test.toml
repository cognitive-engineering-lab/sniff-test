[panics]
panic-sink-namespaces = [
    "core::panicking::**",
    "std::panicking::**",
    "core::std::rt::panic_fmt",
    "std::rt::panic_fmt",
]

[panics.lints]
cached-dependency-panic = "deny"

[safety]
ignored-namespaces = ["dependency_generic_private_panic::**"]
