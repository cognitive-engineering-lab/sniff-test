[analysis]
report-roots = ["dependency_closure_app::nested"]

[compiler]
inline-mir = "off"

[panic]
trusted-boundary-namespaces = ["core", "std"]
[safety]
trusted-boundary-namespaces = ["core", "std"]
[panic.lints]
operation = "deny"
invocation = "deny"
