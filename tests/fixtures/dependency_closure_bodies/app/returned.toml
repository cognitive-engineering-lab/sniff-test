[analysis]
report-roots = ["dependency_closure_app::returned"]

[compiler]
inline-mir = "off"

[panics]
trusted-boundary-namespaces = ["core", "std"]
[safety]
trusted-boundary-namespaces = ["core", "std"]
[panics.lints]
operation = "deny"
invocation = "deny"
