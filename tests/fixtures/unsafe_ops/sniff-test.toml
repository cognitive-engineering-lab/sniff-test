# Non-call unsafe operations must demand `// SAFETY:` justifications the same
# way unsafe calls do.

[analysis]
report-roots = "all"

[panics.lints]
# This fixture exercises safety-comment coverage. Raw pointer/static MIR
# assertions are covered by panic-focused fixtures.
missing-docs = "allow"
