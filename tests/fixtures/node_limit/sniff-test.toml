# A traversal that halts at the node limit must fail loudly instead of
# reporting a false clean verdict.

[analysis]
node-limit = 4

[panics]
panic-sink-namespaces = [
    "core::panicking::**",
    "std::panicking::**",
    "core::std::rt::panic_fmt",
    "std::rt::panic_fmt",
]
