[analysis]
report-roots = ["ambiguous_markers::shared_block_marker"]

[analysis.lints]
ambiguous-effect-marker = "allow"

[panics]
panic-sink-namespaces = [
    "core::panicking::**",
    "std::panicking::**",
    "core::std::rt::panic_fmt",
    "std::rt::panic_fmt",
]
