# Calls through generic bounds and function pointers must surface as either
# documented obligations or unverifiable boundaries, never as silence.

[panic]
[panic.lints]
invocation = "warn"

[safety.lints]

[panic.coverage]
unresolved-call-target = "warn"

[safety.coverage]
unresolved-call-target = "warn"
