# A traversal that halts at the trace depth limit must fail loudly instead of
# reporting a false clean verdict.

[analysis]
max-trace-depth = 4

[panics]
panic-sink-namespaces = [
    "core::panicking::**",
    "std::panicking::**",
    "core::std::rt::panic_fmt",
    "std::rt::panic_fmt",
]
