# Vendored code under the config directory is not a workspace member and must
# not be held to workspace deny gating.

[panics]
panic-sink-namespaces = [
    "core::panicking::**",
    "std::panicking::**",
    "core::std::rt::panic_fmt",
    "std::rt::panic_fmt",
]
