[compiler]
inline-mir = "off"

[analysis]
report-roots = [
    "panic_requirements::satisfied",
    "panic_requirements::partially_satisfied",
]

[panics]
panic-sink-namespaces = [
    "core::panicking::**",
    "std::panicking::**",
    "core::std::rt::panic_fmt",
    "std::rt::panic_fmt",
]
